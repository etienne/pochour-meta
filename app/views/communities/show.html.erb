<%= link_to t(:communities), :root, class: :back %>
<h1><%= @community.name %>&nbsp;<%= link_to t(:edit), edit_community_path(@community), class: 'button mini' %></h1>
<ul>
  <% @community.articles.each do |a| %>
  <li><%= link_to a.title, a %></li>
  <% end %>
</ul>