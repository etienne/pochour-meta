<% if user_signed_in? %>
<header>
  <span class="light">Salut</span> <span class="account-menu"><%= current_user.name %></span>
  <div class="account-menu-popup">
    <ul>
      <li><%= link_to t(:account), :edit_user_registration %></li>
      <li><%= link_to t(:sign_out), :destroy_user_session, method: :delete %></li>
    </ul>
  </div>
  <%= link_to t(:write_something), :new_article, class: :button %>
</header>
<% end %>
